<h1>This is the single Car page</h1>

<h3> <strong>CAR Name: <%= @car.name %></strong></h3>
<p><strong>CAR PRICE: £ <%=@car.price.to_f%></strong></p>
<p><strong>Model: <%=@car.model %></strong></p>
<p><strong>Rating: <%=@car.rating%></strong></p>
<p> <strong>Description: <%=@car.description %> </strong></p>

<% @car.bookings.each do |booking| %>
  <%# <%= booking.start_date.end_date %>
 <%= @booking.start_date.to_time.strftime("%e %b %Y") %>
  <%= @booking.end_date.to_time.strftime("%e %b %Y") %>
  <%= link_to 'Delete', booking_path(booking), method: :delete, data: {confirm: 'Are you sure?'}%>
<% end %>

<%= link_to 'Back', cars_path %>
<%= link_to 'Edit-Listing', edit_car_path(@car)%>
<%= link_to 'Book-now', new_car_booking_path(@car)%>
<%= link_to 'Delete', car_path(@car), method: :delete, data: {confirm: 'Are you sure?'}%>
